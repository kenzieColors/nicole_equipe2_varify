import styled from "styled-components";

const StyledUserSavedVars = styled.section`
  border: 2px solid var(--color-brand-4);
  margin: 15px;
  padding: 20px;
  border-radius: var(--radius-default);
`;

export default StyledUserSavedVars;
